// pages/index.tsx
export default function Home() {
  return (
    <div style={{ padding: 40 }}>
      <h1>FriendsNavigator — Home (Pages Router)</h1>
      <p>If you see this, Next is serving pages at the port your server listens on.</p>
      <p>Next: try <a href="/debug">/debug</a> or access the server using the Host:Port shown in the terminal (e.g. http://localhost:5000).</p>
    </div>
  );
}